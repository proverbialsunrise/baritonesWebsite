# baritonesWebsite
Website for the Bearded Baritones.  This website is live at http://beardedbaritones.com.

To run the server:

1. Install node.js from nodejs.org
1. Get the secret config file, and place it in config/index.js
1. Install the project's dependencies: `npm install`
1. Run it: `node server.js`
